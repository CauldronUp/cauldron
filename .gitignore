/cauldron
/cauldron.exe

# Any Go build in the repo root, under whatever name it was given. Thirty-three
# of these were committed at once -- 769MB of the same binary under thirty-three
# names -- because they were built here for probing and swept up by `git add -A`.
# Naming them one at a time is how that happened; this does not.
*.exe
*.exe~
/dist/
*.test
*.out
.DS_Store
