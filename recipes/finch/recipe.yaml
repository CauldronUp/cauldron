# Finch Recipe for Cauldron
#
# Finch is a unified API over payroll and HR systems, and it differs from
# Merge in one way that is worth a Recipe of its own: it tells you what it
# cannot do.
#
# Every connection exposes /provider, which lists the fields that provider
# actually supports. That endpoint is the difference between "this employee
# has no manager" and "this payroll system has never had a concept of
# managers", and both of those arrive as null everywhere else. Almost nobody
# calls it, so almost every Finch integration treats an unsupported field as
# an empty one, and the bug only appears when a customer connects the wrong
# payroll system.
#
# Four more worth modelling exactly:
#
#   1. Batch endpoints answer 200 with per-item failures inside. Asking for
#      four employees can return two employees, one 404 and one 500, in one
#      array, under one successful status code. Code that checks the status
#      and iterates the array processes error objects as if they were people.
#   2. An assisted connection is not an API connection. Finch fills some
#      integrations by having a human log in on a schedule, so the data is
#      days old, writes are not possible, and the only sign is
#      authentication_method on the connection.
#   3. Individual and employment are two objects for one person, fetched
#      from two endpoints, joined on an id that is Finch's rather than the
#      provider's. Neither is complete on its own and neither says so.
#   4. Pay statements are per pay period, not per person, and one statement
#      holds many people's earnings and taxes. A client that iterates
#      statements expecting one person's history iterates the whole company.
#
# Probed live, unauthenticated, on 2026-09-05, against api.tryfinch.com:
#
#   1. GET /providers with no Authorization header at all, and again with an
#      invented Bearer token, both answer 200 with the full, byte-identical
#      provider catalogue -- Finch's own directory of what it integrates
#      with is genuinely public, and ignores the credential rather than
#      merely tolerating its absence. routes.provider.public below carries
#      that (the strict "always" reading, verified the way FireHydrant's
#      Recipe in this collection verifies its own: byte-identical with a
#      junk token attached and with nothing sent at all).
#   2. GET /employer/directory (a route this Recipe does not make public)
#      with no Authorization header answers 401 {"code":401,
#      "name":"unauthorized_request_error","message":"Unauthorized request:
#      no authentication given"}. The same route with an invented Bearer
#      token answers a different 401 entirely: {"code":401,
#      "name":"invalid_token_error","message":"AUTHENTICATION MIDDLEWARE:
#      could not match any resolver for the token format"}. Two distinct
#      sentences for two distinct verdicts, neither the "The access token is
#      invalid" this file declared for authentication_error before this
#      probe. auth.absent_error below carries the split.
#   3. GET /not-a-real-endpoint, with the Finch-API-Version header present
#      and no credential, answers 404 {"code":404,"name":"not_found_error",
#      "finch_code":"item_not_found","message":"Route not found"} -- reached
#      without authentication mattering, which auth.after_routing below
#      declares. PATCH on a real path (/providers or /employer/directory,
#      tried both), same headers, answers the identical body: Finch does not
#      appear to distinguish a wrong method from an unrouted path at all.
#   4. Omitting Finch-API-Version answers 400, on every route including
#      ones that do not exist: {"code":400,"name":"invalid_request_error",
#      "finch_code":"invalid_request","message":"Missing 'Finch-API-Version'
#      header"} -- a different sentence from "The Finch-API-Version header
#      is required", which this file declared before the probe, and a
#      response envelope carrying a fourth field, finch_code, that none of
#      this Recipe's errors modelled at all: point 3's not_found_error
#      carries the same field.
#
# Known fidelity gap: point 4's header check runs before routing is ever
# resolved in the real API -- a garbage path with no version header still
# gets the version complaint, not "route not found". Cauldron ties a
# Recipe's required_headers check to the same auth.after_routing ordering as
# the credential check, and after_routing below is spent on the credential
# question, which matters for more integrations than an unrouted path
# missing a header nobody sends to a path that does not exist. Not modelled;
# stated here rather than guessed around.
#
# Known fidelity gaps, stated rather than hidden:
#
#   - The provider capability list is a fixture rather than a real
#     per-provider table. A real one is per integration and changes as Finch
#     adds coverage, which is the part no Recipe can pin for everybody.
#   - The manager is not modelled. Finch sends "manager": null -- the whole
#     object, not an id inside it -- and this format nests fields under a
#     parent rather than describing the parent as nullable. A Recipe that
#     answered {"manager": {"id": null}} would be a different shape from the
#     one Finch sends, and code branching on manager === null would take the
#     wrong branch. The unsupported-versus-empty trap is carried by
#     middle_name and preferred_name instead, which are genuinely flat.
#   - No syncing. An assisted connection's data is as old as a fixture says.
#   - Payroll writes, benefits and the sandbox's own simulation endpoints
#     are not modelled.
#
# Keys below are fixtures, not secrets, and every figure is invented.

recipe: finch
capability: hr
version: 0.1.0

upstream:
  api: v1
  docs: https://developer.tryfinch.com/api-reference
  # Found by cauldron discover rather than recorded by hand. This
  # document declares three of the five routes this Recipe models, which is
  # evidence it belongs to this provider rather than proof.
  spec: https://api.tryfinch.com/openapi.json
  spec_hash: fa1fbd6d066a5ec2aeee6c386a6b70635b550469e4ad912724d71b530ae7459f
  spec_seen: "2026-09-05"

auth:
  scheme: bearer
  header: Authorization
  prefix: "Bearer "
  # Struck live: no credential at all names the absence explicitly, a
  # different sentence and a different name from a present-but-wrong one.
  # See the header.
  absent_error: no_authentication
  # Struck live with no credential at all: an unrouted path and a wrong
  # method both answer before authentication is ever consulted. See the
  # header for the header-ordering gap this trades against.
  after_routing: true
  keys:
    - cauldron-finch-access-token

required_headers:
  Finch-API-Version:
    error: missing_api_version

responses:
  list:
    style: wrapped
    # A bare array under "responses" for batch reads, and "individuals" or
    # "paging" shapes elsewhere. Each resource names its own.
  resource:
    style: bare
  error:
    style: flat
    message_field: message
    code_field: code
    type_field: name

resources:
  # What this connection can actually do. The endpoint almost nobody calls,
  # and the only thing that distinguishes an unsupported field from an empty
  # one.
  provider:
    collection: providers
    id:
      style: opaque
      length: 12
    fields:
      display_name:
        type: string
      # assisted or api. An assisted connection is filled by a human logging
      # in on a schedule, so the data is days old and writes are impossible,
      # and this is the only place that shows.
      authentication_methods:
        type: list
      # The fields this provider supports. A field absent from here is null
      # in every response and will be null forever, which is a different
      # thing from a field that happens to be empty for one employee.
      supported_fields:
        type: list
      manual:
        type: boolean
        default: false

  # Who somebody is. Half of a person: the other half is their employment,
  # from a different endpoint, joined on an id that is Finch's rather than
  # the provider's.
  individual:
    collection: responses
    id:
      style: uuid
    fields:
      first_name:
        type: string
      last_name:
        type: string
      # Null because this provider does not expose it, or null because it is
      # not set. /provider is the only way to tell, and this response says
      # nothing.
      middle_name:
        type: string
        null_when_unset: true
      preferred_name:
        type: string
        null_when_unset: true
      dob:
        type: string
        null_when_unset: true
      # Only ever the last four. A client expecting a full number to
      # validate against gets four digits and no indication that is all
      # there is.
      ssn:
        type: string
        null_when_unset: true
      gender:
        type: string
        null_when_unset: true
      email:
        type: string
        in: emails[0]
        as: data
      phone:
        type: string
        in: phone_numbers[0]
        as: data

  # The other half. Employment dates, department, manager, and the fields a
  # payroll system may simply not have.
  employment:
    collection: responses
    id:
      style: uuid
    fields:
      first_name:
        type: string
      last_name:
        type: string
      # employee or contractor, and this is the field most likely to be
      # missing entirely: several payroll systems have no such distinction.
      employment_type:
        type: string
        in: employment
        as: type
      employment_subtype:
        type: string
        in: employment
        as: subtype
      start_date:
        type: string
        null_when_unset: true
      end_date:
        type: string
        null_when_unset: true
      is_active:
        type: boolean
        default: true
      department_name:
        type: string
        in: department
        as: name
      # Amount in cents and a unit, because a salary and an hourly rate are
      # the same field with a different unit and comparing them without it
      # compares an hourly rate to an annual salary.
      income_amount:
        type: integer
        in: income
        as: amount
      income_unit:
        type: string
        in: income
        as: unit
      income_currency:
        type: string
        in: income
        as: currency

  # One pay period for the whole company. Not one person's payslip.
  pay_statement:
    collection: paging_items
    id:
      style: uuid
      field: payment_id
    fields:
      individual_id:
        type: string
      gross_pay_amount:
        type: integer
        in: gross_pay
        as: amount
      net_pay_amount:
        type: integer
        in: net_pay
        as: amount
      payment_method:
        type: string
      total_hours:
        type: number
        null_when_unset: true

routes:
  # The endpoint almost nobody calls, and the only thing that tells an
  # unsupported field from an empty one. Struck live: genuinely public,
  # answering byte-identically with no credential and with an invented one.
  # See the header.
  - method: GET
    path: /providers
    resource: provider
    operation: list
    pagination:
      # Finch's description declares no query parameters on this route at
      # all. It is the route that answers without a credential -- struck
      # live on 2026-09-05 -- and it answers the whole list of providers.
      #
      # So this listing does not page: no page size, no position, no pointer
      # to a next page.
      style: none
    public: true

  - method: GET
    path: /employer/directory
    resource: employment
    operation: list

    pagination:
      # Finch's own description declares limit and offset here, and puts
      # both numbers in the parameter's own words: "Number of employees to
      # return (defaults to 100, maximum 10000)" and "Index to start from
      # (defaults to 0)".
      style: offset
      limit: 100
      max_limit: 10000
      limit_param: limit
      cursor_param: offset
  - method: GET
    path: /employer/individual/{id}
    resource: individual
    operation: get

  - method: GET
    path: /employer/employment/{id}
    resource: employment
    operation: get

  - method: GET
    path: /employer/pay-statement
    resource: pay_statement
    operation: list
    pagination:
      # The body is a requests array naming the statements wanted, from
      # Finch's own openapi.json read 2026-09-05, and the only query
      # parameter is entity_ids. The caller enumerates what it wants, so
      # there is nothing to size and nowhere to move.
      #
      # So this does not page, and the runtime was serving ten of it while
      # reading a "limit" nobody sends.
      style: none

errors:
  rate_limit:
    status: 429
    code: 429
    type: rate_limit_exceeded
    message: "Too many requests"

  # Struck live with an invented Bearer token, present and wrong.
  authentication_error:
    status: 401
    code: 401
    type: invalid_token_error
    message: "AUTHENTICATION MIDDLEWARE: could not match any resolver for the token format"

  # Struck live with no Authorization header at all: a different name and a
  # different sentence from a present-but-wrong token.
  no_authentication:
    status: 401
    code: 401
    type: unauthorized_request_error
    message: "Unauthorized request: no authentication given"

  # Struck live with no credential at all: the router answers before
  # authentication is ever consulted, which auth.after_routing declares.
  # finch_code is a fourth field this Recipe's errors had not modelled.
  unknown_route:
    status: 404
    code: 404
    type: not_found_error
    message: "Route not found"
    fields:
      finch_code: item_not_found

  # Struck live against a real path with a method this Recipe does not
  # declare for it: the identical body an unrouted path gets, not a
  # distinct 405.
  method_not_allowed:
    status: 404
    code: 404
    type: not_found_error
    message: "Route not found"
    fields:
      finch_code: item_not_found

  resource_missing:
    status: 404
    code: 404
    type: not_found
    message: "The requested resource was not found"

  parameter_missing:
    status: 400
    code: 400
    type: invalid_request
    message: "The request is missing a required parameter"

  # No Finch-API-Version header. Struck live: a different sentence from "The
  # Finch-API-Version header is required", this file's guess before the
  # probe, and a response carrying a fourth field, finch_code, alongside it.
  # 400 rather than 426 or 400-with-a-hint, and the version is pinned per
  # request rather than per key, so forgetting it is a runtime failure
  # rather than a configuration one.
  missing_api_version:
    status: 400
    code: 400
    type: invalid_request_error
    message: "Missing 'Finch-API-Version' header"
    fields:
      finch_code: invalid_request

  # This provider does not have this data at all. Not empty: absent, forever,
  # and the fix is asking the customer to connect something else.
  not_supported:
    status: 501
    code: 501
    type: not_implemented
    message: "This provider does not support this endpoint"

  # An assisted connection cannot be read on demand. Finch fills it by
  # having a human log in on a schedule, so a request outside that schedule
  # answers with what it has or with this.
  connection_pending:
    status: 202
    code: 202
    type: sync_pending
    message: "The connection is still being set up and data is not available yet"

fixtures:
  empty: {}

  small-employer:
    provider:
      # An API connection with most fields. manager is not in the supported
      # list, so every employment here has a null manager and always will.
      - id: gusto
        display_name: Gusto
        authentication_methods:
          - api
        supported_fields:
          - individual.first_name
          - individual.last_name
          - individual.dob
          - individual.emails
          - employment.employment_type
          - employment.start_date
          - employment.department
          - employment.income
        manual: false

      # Assisted. A human logs in on a schedule, so the data is days old and
      # writes are impossible, and this list is the only place that shows.
      - id: paychex_flex
        display_name: Paychex Flex
        authentication_methods:
          - assisted
        supported_fields:
          - individual.first_name
          - individual.last_name
          - employment.start_date
        manual: true

    individual:
      # dob is supported and set. middle_name and preferred_name are not
      # supported by this provider, so they are null and will stay null, and
      # nothing here says which of the two reasons applies.
      - id: 1f8a4c30-6b25-4e97-a013-5d72c9f40e68
        first_name: Ada
        last_name: Okonkwo
        dob: "1991-04-17"
        ssn: "6789"
        email: ada@example.com
        phone: "+15551112222"

    employment:
      - id: 1f8a4c30-6b25-4e97-a013-5d72c9f40e68
        first_name: Ada
        last_name: Okonkwo
        employment_type: employee
        employment_subtype: full_time
        start_date: "2024-03-04"
        is_active: true
        department_name: Operations
        # 96000000 cents a year, which is 960,000.00 in whatever currency
        # income_currency says. The unit is the whole meaning: the same
        # number under "hourly" would be an absurd wage.
        income_amount: 9600000
        income_unit: yearly
        income_currency: USD

      # A contractor paid hourly. The same income_amount shape, a different
      # unit, and comparing the two numbers without the unit compares an
      # hourly rate to an annual salary.
      - id: 7c2e9b51-4d68-4a03-b17f-3e95a60c8d24
        first_name: Bede
        last_name: Marchetti
        employment_type: contractor
        employment_subtype: null
        start_date: "2025-11-02"
        is_active: true
        department_name: Engineering
        income_amount: 9500
        income_unit: hourly
        income_currency: USD

    pay_statement:
      # One pay period, two people. A client iterating statements expecting
      # one person's history iterates the whole company.
      - id: 3d0f7a62-8c14-4b59-9e26-1a83f5d70c49
        individual_id: 1f8a4c30-6b25-4e97-a013-5d72c9f40e68
        gross_pay_amount: 400000
        net_pay_amount: 289400
        payment_method: direct_deposit
        total_hours: 80.0

      - id: 5b3c8e17-2a09-4f76-8d41-6c92b0e35a87
        individual_id: 7c2e9b51-4d68-4a03-b17f-3e95a60c8d24
        gross_pay_amount: 152000
        net_pay_amount: 152000
        payment_method: check

# The resource cases below are derived from Finch's published documentation
# rather than a real connection -- no account for one was ever created. The
# refusal cases carrying a verified date were struck live, unauthenticated,
# against api.tryfinch.com; see the header.
conformance:
  - name: the provider list says what this connection cannot do
    # employment.manager is not in the supported list, so every manager on
    # this connection is null and always will be. That is a different fact
    # from an employee having no manager, and no other endpoint distinguishes
    # them.
    source: https://developer.tryfinch.com/api-reference/providers
    fixture: small-employer
    request:
      method: GET
      path: /providers
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        providers[0].id: gusto
        providers[0].supported_fields[0]: individual.first_name
      absent:
        - providers[2]

  - name: an assisted connection is not an API connection
    # A human logs in on a schedule, so the data is days old and writes are
    # impossible. authentication_methods is the only place that shows, and
    # its supported field list is a third the length.
    source: https://developer.tryfinch.com/api-reference/providers
    fixture: small-employer
    request:
      method: GET
      path: /providers
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        providers[1].id: paychex_flex
        providers[1].authentication_methods[0]: assisted
        providers[1].manual: true
        providers[0].authentication_methods[0]: api
        providers[0].manual: false

  - name: an unsupported field and an empty one are both null
    # middle_name and preferred_name are absent from this provider's
    # supported list, so they are null here and will be null forever. dob is
    # supported and set. The response looks the same either way.
    source: https://developer.tryfinch.com/api-reference/hris/individual
    fixture: small-employer
    request:
      method: GET
      path: /employer/individual/1f8a4c30-6b25-4e97-a013-5d72c9f40e68
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        first_name: Ada
        dob: "1991-04-17"
        middle_name: null
        preferred_name: null
        gender: null

  - name: the ssn is four digits and that is all there is
    # A client expecting a full number to validate against gets the last four
    # and nothing saying that is the whole field.
    source: https://developer.tryfinch.com/api-reference/hris/individual
    fixture: small-employer
    request:
      method: GET
      path: /employer/individual/1f8a4c30-6b25-4e97-a013-5d72c9f40e68
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        ssn: "6789"

  - name: an email is an object in an array, not a string
    # emails[0].data, because a person can have several and each carries a
    # type. Reading individual.email finds nothing.
    source: https://developer.tryfinch.com/api-reference/hris/individual
    fixture: small-employer
    request:
      method: GET
      path: /employer/individual/1f8a4c30-6b25-4e97-a013-5d72c9f40e68
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        emails[0].data: ada@example.com
        phone_numbers[0].data: "+15551112222"
      absent:
        - email
        - phone

  - name: income is an amount and a unit, and the unit is the meaning
    # 9600000 cents yearly and 9500 cents hourly. Comparing the two numbers
    # without the unit compares an hourly rate to an annual salary and gets
    # the answer exactly backwards.
    source: https://developer.tryfinch.com/api-reference/hris/employment
    fixture: small-employer
    request:
      method: GET
      path: /employer/directory
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        responses[0].income.amount: 9600000
        responses[0].income.unit: yearly
        responses[1].income.amount: 9500
        responses[1].income.unit: hourly
      absent:
        - responses[2]
        - responses[0].income_amount

  - name: employment type is nested and its subtype can be null
    # A contractor with no subtype, which several payroll systems have no
    # concept of at all.
    source: https://developer.tryfinch.com/api-reference/hris/employment
    fixture: small-employer
    request:
      method: GET
      path: /employer/employment/7c2e9b51-4d68-4a03-b17f-3e95a60c8d24
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        employment.type: contractor
        employment.subtype: null
        department.name: Engineering
      absent:
        - employment_type
        - employment_subtype

  - name: an active employment has no end date and says so with null
    # Present and null rather than left out, so a client testing for the
    # key's existence finds it on everybody and has to read the value.
    source: https://developer.tryfinch.com/api-reference/hris/employment
    fixture: small-employer
    request:
      method: GET
      path: /employer/employment/1f8a4c30-6b25-4e97-a013-5d72c9f40e68
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        end_date: null
        is_active: true
        start_date: "2024-03-04"

  - name: a pay statement is a pay period, not a person
    # One period, two people, and the individual each row belongs to is a
    # field on the row. A client iterating statements expecting one person's
    # history iterates the whole company.
    source: https://developer.tryfinch.com/api-reference/hris/pay-statement
    fixture: small-employer
    request:
      method: GET
      path: /employer/pay-statement
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        paging_items[0].individual_id: 1f8a4c30-6b25-4e97-a013-5d72c9f40e68
        paging_items[1].individual_id: 7c2e9b51-4d68-4a03-b17f-3e95a60c8d24
        paging_items[0].gross_pay.amount: 400000
        paging_items[0].net_pay.amount: 289400
      absent:
        - paging_items[2]

  - name: a contractor's gross and net are the same and hours are absent
    # Nothing was withheld and nobody counted hours. total_hours is present
    # and null rather than zero, which is the difference between "worked no
    # hours" and "hours are not tracked for this person".
    source: https://developer.tryfinch.com/api-reference/hris/pay-statement
    fixture: small-employer
    request:
      method: GET
      path: /employer/pay-statement
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        paging_items[1].gross_pay.amount: 152000
        paging_items[1].net_pay.amount: 152000
        paging_items[1].total_hours: null
        paging_items[0].total_hours: 80.0

  - name: the API version header is required on every request
    # Struck live: a different sentence from this file's guess before the
    # probe, and a fourth field, finch_code, this Recipe now carries.
    # Pinned per request rather than per key, so forgetting it is a runtime
    # failure rather than a configuration one.
    source: https://api.tryfinch.com/providers
    verified: "2026-09-05"
    fixture: small-employer
    request:
      method: GET
      path: /providers
      headers:
        Authorization: Bearer cauldron-finch-access-token
    expect:
      status: 400
      body:
        message: "Missing 'Finch-API-Version' header"
        name: invalid_request_error
        finch_code: invalid_request

  - name: an endpoint this provider lacks is a 501, not an empty list
    # Not empty: absent, forever, and the fix is asking the customer to
    # connect something else.
    source: https://developer.tryfinch.com/api-reference/development-guides/Errors
    fixture: small-employer
    arm: not_supported
    request:
      method: GET
      path: /employer/directory
      headers:
        Authorization: Bearer cauldron-finch-access-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 501
      body:
        name: not_implemented

  - name: an invalid token says so with a name as well as a code
    # Struck live against /employer/directory, a route this Recipe does not
    # make public -- unlike /providers, below, a wrong token here is refused.
    source: https://api.tryfinch.com/employer/directory
    verified: "2026-09-05"
    fixture: small-employer
    request:
      method: GET
      path: /employer/directory
      headers:
        Authorization: Bearer wrong
        Finch-API-Version: "2020-09-17"
    expect:
      status: 401
      body:
        code: 401
        name: invalid_token_error
        message: "AUTHENTICATION MIDDLEWARE: could not match any resolver for the token format"

  - name: no credential at all names the absence explicitly
    # Struck live against /employer/directory. A different name and a
    # different sentence from a present-but-wrong token above.
    source: https://api.tryfinch.com/employer/directory
    verified: "2026-09-05"
    fixture: small-employer
    request:
      method: GET
      path: /employer/directory
      headers:
        Finch-API-Version: "2020-09-17"
    expect:
      status: 401
      body:
        code: 401
        name: unauthorized_request_error
        message: "Unauthorized request: no authentication given"

  - name: the provider catalogue is public and ignores the credential entirely
    # Struck live: byte-identical with no credential and with an invented
    # Bearer token, which is the "always" reading of public rather than
    # "only when nothing was presented" -- verified the way FireHydrant's
    # Recipe in this collection verifies its own.
    source: https://api.tryfinch.com/providers
    verified: "2026-09-05"
    fixture: small-employer
    request:
      method: GET
      path: /providers
      headers:
        Authorization: Bearer totally-invented-wrong-token
        Finch-API-Version: "2020-09-17"
    expect:
      status: 200
      body:
        providers[0].id: gusto

  - name: a path this API does not route needs no credential either
    # Struck live with no credential at all. The router answers before
    # authentication is ever consulted, which auth.after_routing declares.
    source: https://api.tryfinch.com/not-a-real-endpoint
    verified: "2026-09-05"
    fixture: empty
    request:
      method: GET
      path: /not-a-real-endpoint
      headers:
        Finch-API-Version: "2020-09-17"
    expect:
      status: 404
      body:
        code: 404
        name: not_found_error
        finch_code: item_not_found
        message: "Route not found"

  - name: a wrong method on a real path is the identical route-not-found body
    # PATCH on /providers, a method this Recipe does not declare for it.
    # Struck live with no credential at all.
    source: https://api.tryfinch.com/providers
    verified: "2026-09-05"
    fixture: empty
    request:
      method: PATCH
      path: /providers
      headers:
        Finch-API-Version: "2020-09-17"
    expect:
      status: 404
      body:
        name: not_found_error
        finch_code: item_not_found
        message: "Route not found"

  # The page size, on a fixture of two. Asking for one and receiving the
  # first without the second can only happen if limit was read -- with
  # the name ignored the whole collection comes back, because the whole
  # collection is two records. No third record is needed for that, and
  # none is invented.
  - name: "limit is read, because a page of one out of two is not the whole collection"
    source: "https://developer.tryfinch.com/api-reference/hris/employment"
    fixture: small-employer
    request:
      method: GET
      path: "/employer/directory"
      query:
        "limit": "1"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 200
      body:
        "responses[0].id": "1f8a4c30-6b25-4e97-a013-5d72c9f40e68"
      absent:
        - "responses[1]"

  # And the position. Asking for one page-worth from offset=1 answers
  # the *second* record, which can only happen if the name was read.
  # Between them these two cases pin both halves; either name renamed to
  # what the runtime falls back on fails one of them.
  - name: "offset moves the page on, and the second record is what comes back"
    source: "https://developer.tryfinch.com/api-reference/hris/employment"
    fixture: small-employer
    request:
      method: GET
      path: "/employer/directory"
      query:
        "limit": "1"
        "offset": "1"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 200
      body:
        "responses[0].id": "7c2e9b51-4d68-4a03-b17f-3e95a60c8d24"
      absent:
        - "responses[1]"

  # The names. This Recipe declares 2 field(s) on a employment that no
  # case asserted, so each of them could have been renamed to anything
  # and every case would have stayed green. The runtime builds the
  # response out of the declaration and the fixture, so this seeds one
  # and asserts the 2 name(s) that came back at the paths they came back
  # under.
  - name: "an employment carries the field names this Recipe declares"
    source: "https://developer.tryfinch.com/api-reference/hris/employment"
    fixture: small-employer
    request:
      method: GET
      path: "/employer/directory"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 200
      body:
        "responses[0].income.currency": "USD"
        "responses[0].last_name": "Okonkwo"

  # The names. This Recipe declares 1 field(s) on a individual that no
  # case asserted, so each of them could have been renamed to anything
  # and every case would have stayed green. The runtime builds the
  # response out of the declaration and the fixture, so this seeds one
  # and asserts the 1 name(s) that came back at the paths they came back
  # under.
  - name: "an individual carries the field names this Recipe declares"
    source: "https://developer.tryfinch.com/api-reference/hris/individual"
    fixture: small-employer
    request:
      method: GET
      path: "/employer/individual/1f8a4c30-6b25-4e97-a013-5d72c9f40e68"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 200
      body:
        "last_name": "Okonkwo"

  # The names. This Recipe declares 1 field(s) on a pay_statement that
  # no case asserted, so each of them could have been renamed to
  # anything and every case would have stayed green. The runtime builds
  # the response out of the declaration and the fixture, so this seeds
  # one and asserts the 1 name(s) that came back at the paths they came
  # back under.
  - name: "a pay_statement carries the field names this Recipe declares"
    source: "https://developer.tryfinch.com/api-reference/hris/pay-statement"
    fixture: small-employer
    request:
      method: GET
      path: "/employer/pay-statement"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 200
      body:
        "paging_items[0].payment_method": "direct_deposit"

  # The names. This Recipe declares 1 field(s) on a provider that no
  # case asserted, so each of them could have been renamed to anything
  # and every case would have stayed green. The runtime builds the
  # response out of the declaration and the fixture, so this seeds one
  # and asserts the 1 name(s) that came back at the paths they came back
  # under.
  - name: "a provider carries the field names this Recipe declares"
    source: "https://developer.tryfinch.com/api-reference/providers"
    fixture: small-employer
    request:
      method: GET
      path: "/providers"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 200
      body:
        "providers[0].display_name": "Gusto"

  # The names. This Recipe declares 1 field(s) on an employment that no
  # case asserted, so each of them could have been renamed to anything
  # and every case would have stayed green. The runtime builds the
  # response out of the declaration and the fixture, so this seeds one
  # and asserts the 1 name(s) that came back at the paths they came back
  # under.
  - name: "an employment carries the field names this Recipe declares, and 1 more of them"
    source: "https://developer.tryfinch.com/api-reference/hris/employment"
    fixture: small-employer
    request:
      method: GET
      path: "/employer/directory"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 200
      body:
        "responses[0].first_name": "Ada"

  # What this provider says when it refuses. The Recipe declares a
  # status, a code and a message for rate limit and nothing asserted any
  # of them, so the wording could have been changed to anything and
  # every case would have stayed green. This makes the refusal happen --
  # arming the rate limit -- and asserts what came back, envelope and
  # all.
  - name: "the rate limit says so in these words"
    source: "https://developer.tryfinch.com/api-reference"
    arm: rate_limit
    fixture: small-employer
    request:
      method: GET
      path: "/providers"
      headers:
        "Authorization": "Bearer cauldron-finch-access-token"
        "Finch-API-Version": "2020-09-17"
    expect:
      status: 429
      body:
        "code": 429
        "message": "Too many requests"
        "name": "rate_limit_exceeded"




