# RAWG Recipe for Cauldron
#
# There are two different sentences for "you did not authenticate", and this
# Recipe can only tell one of them.
#
#   GET /api/games?page_size=1            (no key at all)
#   ->  401  {"error": "The key parameter is not provided"}
#
#   GET /api/games?key=totally_fake_key   (a key, and a wrong one)
#   ->  401  {"error": "The API key is not found"}
#
# Both checked against api.rawg.io on 2026-08-31, both HTTP 401, both the same
# one-field envelope -- and two different sentences depending on whether a key
# was sent at all. Cauldron's own auth check cannot see that difference:
# internal/runtime/auth.go's authorised() returns one bool, and the handler
# raises exactly one error, authentication_error, whichever way it failed.
# That is not a RAWG fact this Recipe forgot to model; it is this project's
# own engine having no branch for "present but wrong" that is different from
# "absent", for any provider, on any scheme. This Recipe declares
# authentication_error as the first sentence -- "The key parameter is not
# provided" -- because that is what a caller with no key at all sees, and the
# second sentence above is real, verified, and cannot be reached through this
# Recipe. Stated rather than forced.
#
# **And the key is checked before the path.** GET /api/nonexistentendpoint
# with no key answers the same {"error": "The key parameter is not provided"},
# not a 404 -- verified the same day. The real RAWG API answers a bad path the
# other way round: an unmatched URL never reaches the view that would check
# the key, so /api/nonexistentendpoint with no key at all comes back 404
# Content-Type text/html, Django's own "Not Found" page, not RAWG's JSON error
# shape. Checked live; also unreachable through this Recipe, for the same
# reason as TMDB's equivalent gap -- Cauldron checks a Recipe's auth before it
# ever attempts to match a route, and RAWG's real stack runs those two checks
# in the opposite order.
#
# Also checked and unremarkable: ?page_size=99999 with no key answers the
# identical "key parameter is not provided" body. Whatever RAWG actually does
# with an oversized page once a request gets past its own auth is not
# observable this way, and RAWG's own OpenAPI description declares page_size
# as a bare integer with no documented maximum -- so no clamp and no over_limit
# is declared here. Unverified stays unverified.
#
# No rate-limit header of any kind appears on api.rawg.io, on any probe above.
#
# Two more, from RAWG's own machine-readable description
# (https://api.rawg.io/docs/?format=openapi -- Swagger 2.0, read rather than
# called; nothing below this point was reachable without a key):
#
#   1. Paging is a page number, and the pointer to the next one is not a
#      token. The listing's own schema requires count and results and declares
#      next and previous as nullable URIs -- both full addresses, not opaque
#      cursors, and both nullable independently of each other. Both are served:
#      next through the cursor field as an absolute URL, previous through
#      prev_field, which the format grew while the Codecov Recipe was being
#      built -- Codecov answers the same four keys, and this paragraph used to
#      end "not expressible in this format today".
#   2. "_count" is not a promise of a number. Of the thirteen fields across
#      Game and GameSingle whose name ends in _count, nine are declared
#      type: integer and four -- reviews_text_count, parent_achievements_count,
#      twitch_count, youtube_count -- are declared type: string, in the same
#      two objects, with nothing in the name to tell which is which. A client
#      that adds one to reviews_text_count concatenates instead.
#
# Known fidelity gaps, stated rather than hidden:
#
#   - No error response of any kind is documented for /games/{id} or /games in
#     RAWG's own OpenAPI description -- only 200. Combined with the auth gate
#     above, there is no way to observe or document what RAWG answers for a
#     game id that does not exist, so no not_found is declared for either
#     route: this Recipe says nothing rather than guessing.
#   - Two games are served, of the real catalogue, and their field values
#     (ratings, counts, identifiers) are illustrative rather than fetched:
#     RAWG's OpenAPI description carries field names and types but no example
#     payloads, and there is no key to call the real endpoints and record one.
#     Two of the actual strings below are the provider's own, though:
#     metacritic_url's documented example is the Witcher 3 Metacritic page,
#     and this Recipe uses exactly that string on exactly that game.
#   - description is declared type: string with no format, so this Recipe does
#     not claim it is HTML, plain text, or Markdown -- unlike TVmaze's summary,
#     nothing here documents which.

recipe: rawg
capability: media
version: 0.1.0

upstream:
  api: v1
  docs: https://api.rawg.io/docs/
  spec: https://api.rawg.io/docs/?format=openapi
  spec_hash: 8f7781d89f3074a1f88b97fea2637a27fb144165cb04f10366c0716eacd48e3d
  spec_seen: "2026-08-31"

auth:
  scheme: query
  param: key
  keys:
    - cauldron-rawg-fixture-key-000000000000

responses:
  list:
    style: wrapped
    key: results
    count_field: count
    # RAWG's own address for the next page, not a token.
    cursor_field: next
    cursor_url: absolute
    cursor_null: true
    # And where you came from: null on the first page, a URL after it. Both
    # are nullable independently of each other in RAWG's own schema.
    prev_field: previous
  resource:
    style: bare
  error:
    # One field, no code, no type, no status echoed back.
    style: flat
    message_field: error
    code_field: "-"

resources:
  # /games. Nineteen fields; nine more appear only on the detail resource
  # below.
  game:
    id:
      style: numeric
      type: number
    fields:
      slug:
        type: string
      name:
        type: string
      released:
        type: string
      tba:
        type: boolean
      background_image:
        type: string
      rating:
        type: number
      rating_top:
        type: integer
      ratings:
        type: map
      ratings_count:
        type: integer
      # Declared type: string in RAWG's own schema, unlike ratings_count
      # beside it.
      reviews_text_count:
        type: string
      added:
        type: integer
      added_by_status:
        type: map
      metacritic:
        type: integer
      playtime:
        type: integer
      suggestions_count:
        type: integer
      updated:
        type: datetime
      # Nullable in RAWG's own schema: a game with no ESRB rating sends null,
      # not an absent key.
      esrb_rating:
        type: map
        null_when_unset: true
      platforms:
        type: list

  # /games/{id}. Everything game has, plus the fields below -- description,
  # website, the reddit_* and *_count fields among them -- that a list hit
  # never carries at all.
  game_single:
    id:
      style: numeric
      type: number
    # /games/{id} takes "An ID or a slug identifying this Game," per RAWG's
    # own description.
    alias: slug
    fields:
      slug:
        type: string
      name:
        type: string
      name_original:
        type: string
      description:
        type: string
      released:
        type: string
      tba:
        type: boolean
      updated:
        type: datetime
      background_image:
        type: string
      background_image_additional:
        type: string
      website:
        type: string
      rating:
        type: number
      rating_top:
        type: integer
      ratings:
        type: map
      reviews_text_count:
        type: string
      added:
        type: integer
      added_by_status:
        type: map
      metacritic:
        type: integer
      metacritic_url:
        type: string
      playtime:
        type: integer
      screenshots_count:
        type: integer
      movies_count:
        type: integer
      creators_count:
        type: integer
      achievements_count:
        type: integer
      # A string here and on reviews_text_count above; an integer on
      # ratings_count and reddit_count below. RAWG's own schema, not this
      # Recipe's invention.
      parent_achievements_count:
        type: string
      reddit_url:
        type: string
      reddit_name:
        type: string
      reddit_description:
        type: string
      reddit_logo:
        type: string
      reddit_count:
        type: integer
      twitch_count:
        type: string
      youtube_count:
        type: string
      ratings_count:
        type: integer
      suggestions_count:
        type: integer
      alternative_names:
        type: list
      parents_count:
        type: integer
      additions_count:
        type: integer
      game_series_count:
        type: integer
      esrb_rating:
        type: map
        null_when_unset: true
      platforms:
        type: list

routes:
  - method: GET
    path: /games
    resource: game
    operation: list
    pagination:
      style: page
      # Not documented anywhere in RAWG's own description as a default; kept
      # small and stated as unverified rather than guessed at a round number
      # that looks more authoritative than it is.
      limit: 20
      limit_param: page_size
      cursor_param: page

  - method: GET
    path: /games/{id}
    resource: game_single
    operation: get

errors:
  # Verified against a request with no key. The second real sentence -- "The
  # API key is not found", for a key that is present and wrong -- cannot be
  # reached through this Recipe; see the header.
  authentication_error:
    status: 401
    message: "The key parameter is not provided"

fixtures:
  empty: {}

  # Illustrative values, not fetched -- see the header for why, and for which
  # two strings below are the provider's own rather than this Recipe's.
  catalogue:
    game:
      - id: "1"
        slug: the-witcher-3-wild-hunt
        name: "The Witcher 3: Wild Hunt"
        released: "2015-05-18"
        tba: false
        background_image: https://media.rawg.io/media/games/illustrative/witcher-3.jpg
        rating: 4.66
        rating_top: 5
        ratings:
          exceptional: 1200
          recommended: 300
        ratings_count: 1500
        reviews_text_count: "12"
        added: 20000
        added_by_status:
          owned: 15000
          playing: 500
        metacritic: 92
        playtime: 30
        suggestions_count: 500
        updated: "2023-11-09T00:00:00"
        esrb_rating:
          id: 4
          slug: mature
          name: Mature
        platforms:
          - platform:
              id: 4
              slug: pc
              name: PC
            released_at: "2015-05-18"

      - id: "2"
        slug: uncharted-4-a-thiefs-end
        name: "Uncharted 4: A Thief's End"
        released: "2016-05-10"
        tba: false
        background_image: https://media.rawg.io/media/games/illustrative/uncharted-4.jpg
        rating: 4.4
        rating_top: 5
        ratings:
          recommended: 800
        ratings_count: 800
        reviews_text_count: "5"
        added: 9000
        added_by_status:
          owned: 8000
          playing: 100
        metacritic: 93
        playtime: 12
        suggestions_count: 200
        updated: "2023-01-01T00:00:00"
        # Nullable, and null on this one: an unrated game, beside a rated one
        # in the same fixture.
        esrb_rating: null
        platforms:
          - platform:
              id: 187
              slug: playstation4
              name: PlayStation 4
            released_at: "2016-05-10"

    game_single:
      - id: "1"
        slug: the-witcher-3-wild-hunt
        name: "The Witcher 3: Wild Hunt"
        name_original: "The Witcher 3: Wild Hunt"
        description: "An award-winning open-world RPG. Illustrative text, not fetched from the live API."
        released: "2015-05-18"
        tba: false
        updated: "2023-11-09T00:00:00"
        background_image: https://media.rawg.io/media/games/illustrative/witcher-3.jpg
        background_image_additional: https://media.rawg.io/media/games/illustrative/witcher-3-alt.jpg
        website: https://thewitcher.com
        rating: 4.66
        rating_top: 5
        ratings:
          exceptional: 1200
          recommended: 300
        reviews_text_count: "12"
        added: 20000
        added_by_status:
          owned: 15000
          playing: 500
        metacritic: 92
        # RAWG's own documented example for this field, verbatim.
        metacritic_url: http://www.metacritic.com/game/playstation-4/the-witcher-3-wild-hunt
        playtime: 30
        screenshots_count: 40
        movies_count: 3
        creators_count: 6
        achievements_count: 78
        parent_achievements_count: "0"
        reddit_url: https://www.reddit.com/r/witcher/
        reddit_name: witcher
        reddit_description: "Illustrative text, not fetched from the live API."
        reddit_logo: https://media.rawg.io/media/games/illustrative/witcher-reddit-logo.png
        reddit_count: 42
        twitch_count: "0"
        youtube_count: "0"
        ratings_count: 1500
        suggestions_count: 500
        alternative_names:
          - Wiedzmin 3
        parents_count: 0
        additions_count: 2
        game_series_count: 3
        esrb_rating:
          id: 4
          slug: mature
          name: Mature
        platforms:
          - platform:
              id: 4
              slug: pc
              name: PC
            released_at: "2015-05-18"

conformance:
  - name: no key at all is refused with one sentence
    source: https://api.rawg.io/api/games?page_size=1
    verified: "2026-08-31"
    fixture: catalogue
    request:
      method: GET
      path: /games
      query:
        page_size: "1"
    expect:
      status: 401
      headers:
        Content-Type: application/json
      body:
        error: "The key parameter is not provided"

  - name: an oversized page_size does not change that either
    source: https://api.rawg.io/api/games?page_size=99999
    verified: "2026-08-31"
    fixture: catalogue
    request:
      method: GET
      path: /games
      query:
        page_size: "99999"
    expect:
      # Still the auth failure: whatever RAWG does with a page this size once
      # a request gets past its own key check is not observable this way.
      status: 401
      body:
        error: "The key parameter is not provided"

  - name: a bad path answers the same body as a bad key, through this engine
    source: https://api.rawg.io/api/nonexistentendpoint
    verified: "2026-08-31"
    fixture: catalogue
    request:
      method: GET
      path: /nonexistentendpoint
    expect:
      # Real RAWG answers this one 404 text/html, because the path never
      # reaches the view that checks the key -- see the header. This case
      # pins what this Recipe's engine actually does, which is check the key
      # first regardless of the path, the same as TMDB's equivalent case.
      status: 401
      body:
        error: "The key parameter is not provided"

  - name: with a key, paging hands back an absolute URL, not a token
    source: https://api.rawg.io/docs/?format=openapi
    fixture: catalogue
    request:
      method: GET
      path: /games
      query:
        key: cauldron-rawg-fixture-key-000000000000
        page_size: "1"
    expect:
      # Documented rather than observed: reaching a 200 needs a key nobody
      # has. count and results.length disagree on purpose -- count is the
      # whole matching set, not this page.
      status: 200
      body:
        count: 2
        # Null on the first page, and present. A client deciding whether it is
        # at the start by testing for the key takes the wrong branch against
        # anything that omits it.
        previous: null
        "results[0].id": 1
        "results[0].slug": the-witcher-3-wild-hunt
      matches:
        # An absolute URL carrying this request moved on to page 2, not an
        # opaque cursor a client would have to concatenate onto something.
        next: '^https?://.*[?&]page=2'
      absent:
        # No second game leaked onto a page of one.
        - "results[1]"


  - name: the last page sends the cursor field as null, not absent
    source: https://api.rawg.io/docs/?format=openapi
    fixture: catalogue
    request:
      method: GET
      path: /games
      query:
        key: cauldron-rawg-fixture-key-000000000000
        page_size: "1"
        page: "2"
      headers: {}
    expect:
      status: 200
      body:
        count: 2
        next: null
        # And on the last page it is the other way round: previous is a URL
        # back to page one and next is the null. Both nullable, independently.
        "results[0].id": 2
      matches:
        previous: '^https?://.*[?&]page=1'

  - name: a game addressed by slug gets the same record as by id
    source: https://api.rawg.io/docs/?format=openapi
    fixture: catalogue
    request:
      method: GET
      path: /games/the-witcher-3-wild-hunt
      query:
        key: cauldron-rawg-fixture-key-000000000000
    expect:
      status: 200
      body:
        id: 1
        name: "The Witcher 3: Wild Hunt"

  - name: four _count fields are strings and the rest are numbers
    source: https://api.rawg.io/docs/?format=openapi
    fixture: catalogue
    request:
      method: GET
      path: /games/1
      query:
        key: cauldron-rawg-fixture-key-000000000000
    expect:
      status: 200
      body:
        id: 1
        # RAWG's own documented example string, on the game it documents it
        # against.
        metacritic_url: http://www.metacritic.com/game/playstation-4/the-witcher-3-wild-hunt
        reviews_text_count: "12"
        parent_achievements_count: "0"
        twitch_count: "0"
        youtube_count: "0"
        # Beside them, on the same record, four counts that really are
        # numbers.
        ratings_count: 1500
        reddit_count: 42
        screenshots_count: 40
        movies_count: 3

  - name: the list shape carries none of the detail-only fields
    source: https://api.rawg.io/docs/?format=openapi
    fixture: catalogue
    request:
      method: GET
      path: /games
      query:
        key: cauldron-rawg-fixture-key-000000000000
        page_size: "10"
    expect:
      status: 200
      body:
        count: 2
        "results[0].name": "The Witcher 3: Wild Hunt"
        "results[1].name": "Uncharted 4: A Thief's End"
        # Nullable and null on this one, beside a rated game on the other.
        "results[1].esrb_rating": null
        "results[0].esrb_rating.name": Mature
      absent:
        - "results[0].description"
        - "results[0].website"
        - "results[0].reddit_url"
        - "results[0].metacritic_url"

  # The page size, on a fixture of two. Asking for one and receiving the
  # first without the second can only happen if page_size was read --
  # with the name ignored the whole collection comes back, because the
  # whole collection is two records. No third record is needed for that,
  # and none is invented.
  - name: "page_size is read, because a page of one out of two is not the whole collection"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
        "page_size": "1"
    expect:
      status: 200
      body:
        "results[0].id": 1
      absent:
        - "results[1]"

  # And the position. Asking for one page-worth from page=2 answers the
  # *second* record, which can only happen if the name was read. Between
  # them these two cases pin both halves; either name renamed to what
  # the runtime falls back on fails one of them.
  - name: "page moves the page on, and the second record is what comes back"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
        "page_size": "1"
        "page": "2"
    expect:
      status: 200
      body:
        "results[0].id": 2
      absent:
        - "results[1]"

  # The names. This Recipe declares 13 field(s) on a game that no case
  # asserted, so each of them could have been renamed to anything and
  # every case would have stayed green. The runtime builds the response
  # out of the declaration and the fixture, so this seeds one and
  # asserts the 10 name(s) that came back at the paths they came back
  # under.
  - name: "a game carries the field names this Recipe declares"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
        "page_size": "1"
    expect:
      status: 200
      body:
        "results[0].added": 20000
        "results[0].background_image": "https://media.rawg.io/media/games/illustrative/witcher-3.jpg"
        "results[0].metacritic": 92
        "results[0].playtime": 30
        "results[0].rating": 4.66
        "results[0].rating_top": 5
        "results[0].released": "2015-05-18"
        "results[0].suggestions_count": 500
        "results[0].tba": false
        "results[0].updated": "2023-11-09T00:00:00"

  # The names. This Recipe declares 27 field(s) on a game_single that no
  # case asserted, so each of them could have been renamed to anything
  # and every case would have stayed green. The runtime builds the
  # response out of the declaration and the fixture, so this seeds one
  # and asserts the 24 name(s) that came back at the paths they came
  # back under.
  - name: "a game_single carries the field names this Recipe declares"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games/1"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
    expect:
      status: 200
      body:
        "achievements_count": 78
        "added": 20000
        "additions_count": 2
        "alternative_names[0]": "Wiedzmin 3"
        "background_image": "https://media.rawg.io/media/games/illustrative/witcher-3.jpg"
        "background_image_additional": "https://media.rawg.io/media/games/illustrative/witcher-3-alt.jpg"
        "creators_count": 6
        "description": "An award-winning open-world RPG. Illustrative text, not fetched from the live API."
        "game_series_count": 3
        "metacritic": 92
        "name_original": "The Witcher 3: Wild Hunt"
        "parents_count": 0
        "playtime": 30
        "rating": 4.66
        "rating_top": 5
        "reddit_description": "Illustrative text, not fetched from the live API."
        "reddit_logo": "https://media.rawg.io/media/games/illustrative/witcher-reddit-logo.png"
        "reddit_name": "witcher"
        "reddit_url": "https://www.reddit.com/r/witcher/"
        "released": "2015-05-18"
        "suggestions_count": 500
        "tba": false
        "updated": "2023-11-09T00:00:00"
        "website": "https://thewitcher.com"

  # The names. This Recipe declares 3 field(s) on a game that no case
  # asserted, so each of them could have been renamed to anything and
  # every case would have stayed green. The runtime builds the response
  # out of the declaration and the fixture, so this seeds one and
  # asserts the 3 name(s) that came back at the paths they came back
  # under.
  - name: "a game carries the field names this Recipe declares, and 3 more of them"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
        "page_size": "1"
    expect:
      status: 200
      body:
        "results[0].added_by_status.owned": 15000
        "results[0].platforms[0].platform.id": 4
        "results[0].ratings.exceptional": 1200

  # The names. This Recipe declares 3 field(s) on a game_single that no
  # case asserted, so each of them could have been renamed to anything
  # and every case would have stayed green. The runtime builds the
  # response out of the declaration and the fixture, so this seeds one
  # and asserts the 3 name(s) that came back at the paths they came back
  # under.
  - name: "a game_single carries the field names this Recipe declares, and 3 more of them"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games/1"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
    expect:
      status: 200
      body:
        "added_by_status.owned": 15000
        "platforms[0].platform.id": 4
        "ratings.exceptional": 1200

  # The names. This Recipe declares 2 field(s) on a game that no case
  # asserted, so each of them could have been renamed to anything and
  # every case would have stayed green. The runtime builds the response
  # out of the declaration and the fixture, so this seeds one and
  # asserts the 2 name(s) that came back at the paths they came back
  # under.
  - name: "a game carries the field names this Recipe declares, and 2 more of them"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
        "page_size": "1"
    expect:
      status: 200
      body:
        "results[0].ratings_count": 1500
        "results[0].reviews_text_count": "12"

  # The names. This Recipe declares 2 field(s) on a game_single that no
  # case asserted, so each of them could have been renamed to anything
  # and every case would have stayed green. The runtime builds the
  # response out of the declaration and the fixture, so this seeds one
  # and asserts the 2 name(s) that came back at the paths they came back
  # under.
  - name: "a game_single carries the field names this Recipe declares, and 2 more of them"
    source: "https://api.rawg.io/docs/?format=openapi"
    fixture: catalogue
    request:
      method: GET
      path: "/games/1"
      query:
        "key": "cauldron-rawg-fixture-key-000000000000"
    expect:
      status: 200
      body:
        "esrb_rating.id": 4
        "esrb_rating.slug": "mature"




